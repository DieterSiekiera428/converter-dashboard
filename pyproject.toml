[project]
name = "ipv4-int-converter"
version = "0.1.0"
description = "Convert IPv4 addresses between dotted-quad string and packed 32-bit integer representations."
requires-python = ">=3.10"
dependencies = []
readme = "README.md"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
